[pytest]
testpaths = evals
markers =
    offline: no network and no model call needed; deterministic forever
    model: needs an API key, but weather is stubbed (no Open-Meteo call)
    paraphrase: real model call, asserts controlled-vocabulary mapping
    live: real model call and real Open-Meteo call
addopts = -ra
